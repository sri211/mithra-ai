services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - FRONTEND_URL=https://mithraai.in
    restart: unless-stopped
    volumes:
      # DATABASE_URL points at /app/data/mithra.db — this mount is what makes
      # user data survive container rebuilds. Do not remove.
      - ./data:/app/data
